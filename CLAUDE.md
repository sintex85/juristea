# Juristea — Law firm management SaaS for Spanish lawyers

Next.js 16 (App Router) + Drizzle ORM + Neon Postgres + NextAuth v5.

Key scripts: `db:generate`, `db:migrate`, `db:push`, `db:studio`, `dev`, `build`, `lint`.
